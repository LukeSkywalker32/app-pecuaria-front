// Placeholder — substitua pelo componente real quando for construir a tela
export default function AnimalDetailPage() {
    return <div style={{ padding: 32 }}>Detalhes do Animal — em construção</div>;
}
